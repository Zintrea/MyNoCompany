# 🚀 MyNoCompany: The Next Gen AI Novel Studio

**(ระบบช่วยเขียนนิยายด้วย Multi-Agent AI ทีมงานนรกแตก)**

> **Note to Future Me:** โปรเจกต์นี้ไม่ใช่แค่ Chatbot ธรรมดา แต่มันคือ **"บริษัทจำลอง"** ที่ใช้ Microsoft AutoGen สร้าง AI 4 ตัวมาเถียงกันเพื่อปั้นพล็อตนิยาย โดยมีระบบจัดการ API Quota แบบ **อมตะ (Infinite Loop)** ที่แกเขียนไว้เองกับมือ!

---

# คำสั่งรัน โปรแกรม
.\venv\Scripts\python.exe Studio.py
# API KEY

# แต่ละคีร์เวิร์ดหมายความว่าอะไร
temperature: ค่าความกาว (0.0-1.0) ยิ่งสูงยิ่งกาว
top_p: ค่าความหลากหลายของคำ (0.0-1.0) ยิ่งสูงยิ่งหลากหลาย
top_k: จำนวนคำที่เลือกจากคำที่มีความน่าจะเป็นสูงสุด    
max_output_tokens: จำนวนคำสูงสุดที่โมเดลจะตอบกลับมา (ยิ่งสูงยิ่งเปลืองโควตา)


---
## 📂 โครงสร้างไฟล์ (File Structure)

โปรเจกต์นี้แบ่งออกเป็น 3 ไฟล์หลัก เพื่อความง่ายในการแก้ไข (Modular Design):

1. **`Studio.py` (The Main Stage)**
* 🎬 **หน้าที่:** เป็นไฟล์หลักสำหรับกด Run (`python Studio.py`)
* **การทำงาน:** เรียก Agents มาเข้าห้องประชุม, รับ Input จากเรา, และเริ่ม Kick-off โปรเจกต์
* **Logic สำคัญ:** มีระบบ Long-term Memory (อ่าน/เขียนไฟล์ `company_database.txt`) เพื่อจำบทเรียนจากนิยายเรื่องเก่า


2. **`Agents.py` (The HR Department)**
* 👥 **หน้าที่:** เก็บ "Character Setting" (System Message) ของพนักงานทุกคน
* **พนักงาน:**
* **Admin May:** เลขาฯ ผู้คุมกฎ (User Proxy)
* **Data Analyst:** ปากร้าย เน้นสถิติและความจริง (Strict Mode)
* **Editor Khem:** บก. จอมเฮี้ยบ เน้นตรรกะ (Logical Mode)
* **Writer Jinta:** นักเขียนสายติสท์ สำนวนสวย (Creative Mode)




3. **`Config.py` (The Engine Room & Hacks)**
* ⚙️ **หน้าที่:** หัวใจสำคัญที่สุด! เก็บ API Keys, การตั้งค่า Model, และ **Code พิสดาร** ทั้งหมด
* **Hacks ที่ใส่ไว้:**
* **Monkey Patching:** การแก้ไส้ใน Library Google ให้รอ Quota ได้
* **Logger:** ระบบแยกท่อ (หน้าจอมีสี / ไฟล์ Log ไม่มีสี)
* **Infinite Fallback:** ลูปนรกเรียกใช้โมเดลวนไปเรื่อยๆ จนกว่าจะติด





---

## 🛠️ ฟีเจอร์ลับที่แกทำไว้ (System Logic)

### 1. ระบบ "วนลูปไม่รู้จบ" (Infinite Model Rotation)

แกแก้ปัญหา `Quota Exceeded (429)` ด้วยการสร้าง **Fallback List** ที่ยาวเหยียด

* **ลำดับการทำงาน:** Gemini 3 Pro -> Flash -> Lite -> Gemma (ตัวสุดท้าย)
* **เมื่อถึงตัวสุดท้าย:** ถ้า Gemma ยังเต็มอีก ระบบจะไม่ยอมแพ้!
* มันจะขึ้นข้อความ `♻️ จบรอบการลองโมเดล! ... Resting...`
* นับถอยหลัง 30 วินาที
* **โยน Error หลอกๆ** ออกมา เพื่อให้ AutoGen เข้าใจว่าตัวสุดท้ายใช้ไม่ได้ แล้ววนกลับไปเริ่มที่ **Gemini 3 Pro** ใหม่ในรอบถัดไป
* *ผลลัพธ์:* โปรแกรมไม่มีวันตาย รันไปได้เรื่อยๆ จนกว่า Google จะยอม



### 2. ระบบ "กันเนื้อหาปลิว" (Safety Airbag)

ถ้าเขียนฉากโหด/โป๊เกินไปจนโดน Google Block (`BlockedPromptException`):

* โค้ดใน `Config.py` จะดักจับ Error นี้ไว้
* มันจะ **ไม่** ทำให้โปรแกรมเด้งหลุด
* แต่มันจะส่ง **Dummy Response** กลับไปว่า `[SYSTEM]: เนื้อหาส่วนนี้รุนแรงเกินนโยบายความปลอดภัย (Censored)`
* เพื่อให้ทีมงานคนอื่นคุยต่อได้โดยไม่สะดุด

### 3. ระบบ "แยกสี Log" (Dual Stream Logger)

* **Terminal:** แสดงตัวหนังสือสีเขียว/แดง/ฟ้า ตามปกติ (อ่านง่ายตอนรัน)
* **meeting_log.txt:** ตัดโค้ดสี (ANSI Codes) ทิ้งทั้งหมด เพื่อให้เปิดอ่านใน Notepad ได้รู้เรื่อง

---

## 🚀 วิธีใช้งาน (How to Run)

1. **เช็ค API Key:**
* เปิดไฟล์ `Config.py`
* ดูตัวแปร `API_KEYS` ว่าใส่ Key ครบหรือยัง (ถ้า Key เก่าหมดอายุ ให้ไปเจนใหม่ที่ Google AI Studio)


2. **รันโปรแกรม:**
```bash
# อย่าลืม Activate venv ก่อนนะ!
source venv/bin/activate  # หรือ venv\Scripts\activate ใน Windows
python Studio.py

```


3. **ขั้นตอนการเล่น:**
* ใส่ไอเดียนิยายที่อยากได้
* นั่งดูพวกมันเถียงกัน (ถ้ามันนิ่งไป คือมันกำลังรอ Quota อยู่นะ ใจเย็นๆ ดูที่ตัวนับถอยหลัง)
* พอจบงาน: กด `y` เพื่อบันทึกพล็อตเก็บเข้า Database



---

## ⚠️ ปัญหาที่อาจเจอ (Troubleshooting)

* **Q: โปรแกรมค้างที่ `Cooling down...` นานมาก?**
* **A:** ปกติครับ มันกำลังรอรอบใหม่ ถ้าเน็ตหลุดหรือ Google ล่มจริงๆ ให้กด `Ctrl+C` แล้วรันใหม่


* **Q: ทำไม Data Analyst ไม่ค่อยด่า?**
* **A:** ลองไปปรับ `temperature` ใน `Config.py` (ตัวแปร `config_strict`) ให้ต่ำลงอีก หรือไปแก้ System Message ใน `Agents.py` ให้แรงขึ้น


* **Q: อยากเพิ่มโมเดลใหม่ๆ ที่ Google เพิ่งปล่อย?**
* **A:** ไปเพิ่มชื่อโมเดลใน `MODEL_PRIORITY_LIST` ในไฟล์ `Config.py` (ระวัง! ต้องเช็คชื่อโมเดลให้เป๊ะๆ ตาม Docs ของ Google)



---

## 📝 บันทึกถึงตัวเอง (Notes)

* **Date Created:** February 2026
* **Tech Stack:** Python, Microsoft AutoGen, Google Gemini API
* **Warning:** **ห้ามอัปโหลดไฟล์ `Config.py` ที่มี API Key ขึ้น GitHub เด็ดขาด!** (ใช้ `.gitignore` ด้วย)

---

*ขอให้สนุกกับการปั่นนิยายนะตัวฉันในอนาคต! อย่าลืมว่า AI เป็นแค่เครื่องมือ จินตนาการอยู่ที่แก!* ✨